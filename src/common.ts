export type HexProps = {
  w: number // width
  h: number // height
  x: number // translate X
  y: number // translate Y
  s: number // length of a side (radius of circumcircle)
  m: number // mid point
  a: number // length of tip
  b: number // length of tip + body
}

// 60° degress
export const α = Math.PI / 3
